spring:
  datasource:
    url: jdbc:mysql://localhost:3306/catalogolivro
    username: root
    password: gladyson18
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    dialect: mysql

server:
  port: 8080